spring:
  profiles: test
  datasource:
    url: jdbc:h2:mem:test;MODE=MySQL
    driver-class-name: org.h2.Driver
    username: sa
    password:
    h2:
      console:
        enabled: true
  h2:
    console:
      path: /h2-console
      settings:
        trace: true
        web-allow-others: true